const getInteractions = require("../util/getInteractions.");

const interactions = [];


module.exports = {
  name: "Update Interactions",
  execute: async function(interaction, Client) {
    
  },
  addInteraction: {
    name: "update_interactions",
    description: "Update the slash commands for this bot."
  }
};
s for this bot."
  }
};
